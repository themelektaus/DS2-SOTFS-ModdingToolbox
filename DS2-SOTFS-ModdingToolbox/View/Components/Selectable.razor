<div class="selectable @(selected ? "selected" : "")" @onmousedown="onClick">
    @ChildContent
</div>

@code {

    [Parameter] public RenderFragment ChildContent { get; set; }
    [Parameter] public bool selected { get; set; }
    [Parameter] public Action onClick { get; set; }

    void OnClick()
    {
        onClick?.Invoke();
    }
}